# Doit pointer vers l'URL de la racine de l'API de abes-hello-back
# cf https://github.com/abes-esr/abes-hello-back
# Par défaut la valeur http://localhost:8081/ est renseigné.
# Elle correspond à l'URL classiquement utilisée pour 
# développer abes-hello-back sur sa machine en local.
# Remarque: dans l'image Docker de abes-hello-front, cette valeur
# est renseignée à la création du conteneur par un système de placeholder
API_URL=http://localhost:8084/api